ClipShare-master 最终修复包
生成时间: 2026-09-05 04:55
基于: 你上传的完整 ClipShare-master（保留你的 gradle.properties 等本地配置）

【功能修复清单】
1. 息屏断连修复
   亮屏时若通知服务 WebSocket 已死/半开才重连，健康则跳过
   （storage_ws_service.dart 新增 isHealthy；storage_service.dart 重写 onScreenOpened）。

2. WebDAV 上传目录自动创建
   uploadFile 遇目标目录不存在时自动级联创建并重试，不再 403。
   涉及 storage_client.dart / web_dav_client.dart / aliyun_oss_client.dart /
   s3_client.dart / storage_service.dart。

3. 发送/接收进度记录保留 + 失败重发
   - 发送记录在传输开始前创建：对方未连接/离线也会留下"空发送"失败记录
   - done/error 记录保留在进度页，可点"清空已结束记录"批量清除
   - 失败/空发送记录显示红色错误原因 + 🔄重发按钮
   - 重发前弹确认框（显示文件名与目标设备），确认后才发送
   - 记录按"同一状态内新记录排在上面"排序，与历史列表一致
   涉及 syncing_file.dart / syncing_file_progress_service.dart /
   file_sync_handler.dart / storage_service.dart / sync_file_controller.dart /
   sync_file_page.dart / sync_file_status.dart / translation_key.dart /
   zh_cn_translations.dart / en_us_translations.dart。

4. 进度记录持久化（本轮新增）
   - 发送/接收进度记录实时保存到本地文件 syncing_records.json（500ms 防抖写入）
   - 杀后台/重启后记录不丢失：进行中的记录恢复为"失败(已中断)"，可重发
   - 接收方记录重启后自动清除（无法续传）
   - 重发按钮在恢复的记录上依然可用（回调自动重建）
   - 新发送记录显示在列表上方（与历史列表一致）
   涉及 syncing_file.dart / pending_file.dart /
   syncing_file_progress_service.dart。

5. 代码清理（本轮新增）
   - 移除 SyncingFile 及整条调用链中未使用的 BuildContext 参数
     （file_sync_handler / socket_service / splash_controller /
     pending_file_service / storage_service / sync_file_status /
     sync_file_controller）
   - 删除无调用方的冗余方法：clearAll / resetForRetry / service.retry
   - 删除未使用的 import（transport_protocol.dart 等）

6. 设备"禁用"功能
   设备卡片可禁用某设备，禁用后不参与自动连接与发送。
   涉及 device.dart / device_dao.dart / app_db.floor.g.dart / db_service.dart /
   device_controller.dart / device_card.dart / socket_service.dart /
   storage_service.dart。

7. Android 构建签名回退
   android/app/build.gradle：存在 key.properties 且 storeFile 有效时用正式签名，
   否则自动回退 debug 签名，无签名配置也能构建 release APK。

8. CI 加速（可选）
   .github/workflows/build-android-arm64.yml：只构建 arm64 单 ABI，
   AOT 编译只跑一遍，产物更小更快。原 build-android.yml 未改动。

【验证】
lib 全部 Dart 代码已用与 CI 相同的工具链（Flutter 3.41.9 / Dart 3.11.5）
完整执行 kernel_snapshot_program 编译，通过、无错误。
临时代码已全部清除（构建标记 BuildMarker 已移除）。
调试/构建标记等临时代码已全部清除，与原版逐行核对。

## 设备禁用功能已完全移除（2026-09-05）
按最终决定，"禁用设备"功能整体删除，代码回到无禁用状态：
- 删除 Device 实体 isDisabled 字段、DeviceDao.updateDeviceDisabled、
  device_card / device_controller 中全部禁用 UI 与逻辑、
  socket_service / storage_service 中全部禁用检查、相关中英文翻译键。
- 数据库版本 11 -> 12：新增迁移自动删除 Device 表 isDisabled 列
  （带列存在性检查，新旧库升级均安全；设备配对数据不受影响）。
- 顺带修复：此前 @Database 注解版本(11)与 floor.g.dart 生成代码版本(10)
  不一致，导致 isDisabled 列迁移从未执行；本次统一为 12。
其余功能（进度持久化、失败重发确认框、新记录排上方、息屏健康重连、
WebDAV 自动建目录、签名回退）全部保留，不受影响。


【2026-09-05】新增：全局"暂停同步"开关
  - 位置：我的设备页标题行，紧靠筛选图标组右侧（设备计数右边）。紧凑开关：开=同步中，关=已暂停。转发状态保持原最右位置。
- 暂停时行为：
  1. 本机新复制的内容不上传（不写同步队列、不写 WebDAV、不通知设备），恢复后也不会补传——关闭期间的隐私内容永不外流。
  2. 收到的内容照常存入历史记录可查，但不自动写入本机剪贴板。
  3. 文件传输、设备连接/配对、心跳在线状态完全不受影响。
- 涉及文件：config_key.dart、config_service.dart、operation_record_dao.dart、history_controller.dart、device_page.dart、translation_key.dart、zh_cn_translations.dart、en_us_translations.dart
- 配置为 key-value 存储，无需数据库版本升级，旧数据无损兼容。


【2026-09-05】新增：历史记录移动端搜索栏滑动消失修复
问题：手机上输入关键字点搜索后，只要滑动列表，顶部搜索栏就退回默认标题栏，
      但过滤条件其实还生效（列表仍是搜索结果），造成"界面变了、搜索还在"的割裂。
根因：搜索框 onTapOutside 在触摸列表滑动时触发 unfocus -> onFocusLost 无条件
      折叠搜索栏（showingHistorySearch=false），而搜索关键字 content 并未清除。
修复：
  1. onFocusLost 改为"仅当无搜索词时才折叠"：有词时只收起键盘、搜索栏保持显示，
     与列表过滤状态一致；滑动不再退回默认界面。
  2. 移动端搜索栏新增"×"关闭按钮：有词时点一下清空搜索并恢复默认列表，
     已空时再点退出搜索模式（符合 Android 搜索框习惯）。桌面端常驻搜索行不受影响。
涉及文件：home_page.dart（_buildHistorySearchAppBar / _HistorySearchAppBar）、
          history_filter.dart（HistoryFilterSearchRow 新增可选 onClose 参数与关闭按钮）。
验证：两文件 dart analyze 无 error/warning，仅存 5 条与本次无关的既有 info。


【2026-09-06】新增：历史记录页返回手势修复 + 全局二次返回退出应用
问题1：在历史记录页搜索后，从屏幕边缘右滑（返回手势）会直接切走整个页面，
      而不是先退出搜索回到默认界面，与 Android 返回键习惯不符。
问题2：应用在任何页面按返回/返回手势都会立即 moveToBg 退到后台，
      没有"再按一次退出"的确认，容易误触。
修复：
  1. 历史记录页处于搜索模式时，返回手势优先：清空搜索关键词、恢复默认列表、
     收起搜索栏并退出搜索模式；仅当不在搜索模式时才继续走原有返回逻辑。
  2. 全局返回改为"二次返回退出"：首次返回提示"再按一次退出应用"，
     2 秒内再次返回才真正退出（SystemNavigator.pop）。多选模式、
     搜索模式等仍优先处理各自的返回，不占用这一次。
涉及文件：home_page.dart（PopScope.onPopInvoked 逻辑）、
          translation_key.dart / zh_cn_translations.dart / en_us_translations.dart
          （新增 pressAgainToExit 文案"再按一次退出应用"/"Press back again to exit"）。
验证：四文件 dart analyze 无 error/warning，仅存 3 条与本次无关的既有 info。


【2026-09-06】调整：移除移动端搜索栏"×"关闭按钮
说明：上一轮(09-05)为移动端搜索栏新增的"×"关闭按钮，与本轮新增的
      "返回手势退出搜索"功能重复，按要求彻底删除其全部代码：
      history_filter.dart 的 onClose 字段/构造参数/按钮 UI 块，
      home_page.dart 的 onClose 回调/_HistorySearchAppBar 相关字段与传参。
      退出搜索统一使用返回手势（清词->回默认列表->收起搜索栏）。
      "有搜索词时滑动不折叠"(onFocusLost)修复保留不变。
涉及文件：history_filter.dart、home_page.dart

【2026-09-07】新增：安卓端接收文件通知（方案A）
说明：原"接收文件后发起通知"仅桌面端生效，安卓端收文件无任何提示。
      现安卓端同样支持，复用已有原生通知通道（AndroidChannelService.sendNotify）。
改动：1. file_sync_handler.dart：_notifyReceivedFileIfNeeded 平台判断由
         "仅桌面端"放宽为"桌面端+安卓端"，仍受设置开关 notifyOnReceivedFile 控制。
      2. settings_notification_page.dart："接收文件后发起通知"开关在安卓端也显示
         （原仅桌面端可见）。
已知限制：安卓端点击通知打开应用主界面（现有原生通道不支持携带文件路径），
          桌面端点击通知直接打开文件的原有行为不变。
验证：两文件 dart analyze 无任何问题。
涉及文件：file_sync_handler.dart、settings_notification_page.dart

【2026-09-07 最终版】回退"通知点击直达文件传输页"方案，改为：
      1. 安卓接收文件通知正文显示"点击查看"（新增翻译键 receivedFileNotifyTapToView，
         中文"点击查看" / 英文"Tap to view"）。点击通知行为回退为原版：仅打开应用。
      2. 设置-通知"接收文件后发起通知"不再显示"点击通知后自动打开文件"描述行。
      3. 安卓端接收文件通知开关生效（isDesktop || isAndroid），桌面端行为不变。
      4. Kotlin 原生代码（MyApplication.kt / MainActivity.kt）已完全回退为原版，
         上一轮交付包中含 openPage 改动的 Kotlin 文件请用本包覆盖，勿再使用。
涉及文件（本条相关）：
  lib/app/data/enums/translation_key.dart
  lib/app/translations/zh_cn_translations.dart
  lib/app/translations/en_us_translations.dart
  lib/app/handlers/sync/file_sync_handler.dart
  lib/app/modules/settings_module/pages/settings_notification_page.dart
验证：dart analyze 无新增问题；Kotlin 与 cs_user 原版逐字节一致。


【2026-09-17】修复：多选浮动按钮（FAB）与删除弹窗的 3 个 UI 问题

问题1：右下角多选 FAB 菜单"折叠状态不统一 + 展开后按钮拥挤"
根因A（折叠态自身不统一）：旧版 clip_multi_selection_fab.dart 里各个子件都用
      **互不相干的硬编码坐标/尺寸**拼在同一 Stack 里：
        - 计数徽标：固定 right:85 / bottom:15 / 高 48
        - 回到顶部：固定 right:15 / bottom:(selectMode?85:15)，用普通
          FloatingActionButton（直径 56）
        - 主 FAB（ExpandableFab）：直径 56，但没有 fanAngle、没有 heroTag
      三者尺寸（48/56/56）、间距（85/15 混用）、对齐基准都不一致，
      加上两个 FAB 均未设置唯一 heroTag，切页/重建时还会互相干扰，
      表现为"折叠态按钮大小不一、位置忽左忽右"。
根因B（展开后拥挤）：主列表（clip_list_fab.dart）写死 distance:145、
      历史弹窗（history_window.dart）写死 distance:70，两处硬编码且不同。
      而两处动作数量不同（主列表 5 个、历史弹窗 3 个），同一半径下
      相邻按钮圆心距 2·R·sin(步长/2) 差别很大：
        主列表 145 → 间距≈56.5px（勉强够）
        历史弹窗  70 → 间距≈53.6px（小于按钮直径 56 → 互相叠住=拥挤）
修复：
  1. 新增静态方法 ClipMultiSelectionFab.suggestDistance(int actionCount)，
     按"相邻按钮圆心距 >= 按钮直径 + 间隙"反推半径：
       R >= spacing / (2·sin(步长/2))，步长 = 90° / (actionCount - 1)，
     叠加主 FAB 半径余量后 clamp 到 [96,168] 防飞出屏幕。
     主列表与历史弹窗都改用 suggestDistance(actions.length)：
       主列表(5) → 168（间距≈65.6px）；历史弹窗(3) → 116.8（间距≈89.4px）。
     两处共用同一规则，历史弹窗不再拥挤，且疏密观感统一。
  2. 统一折叠态布局常量，全部由公共常量推导，不再散落魔法数：
     主 FAB 直径 fabSize=56、屏幕边距 margin=16、徽标高 44/圆角 22、
     徽标与按钮间隙 _badgeGap=12、展开态回到顶部上移 (fabSize+8)。
  3. "回到顶部"改用 FloatingActionButton.small（直径 40），折叠态与主 FAB
     同一底线、展开态上移一格让位扇形。
  4. 补齐 heroes 与手势：主 FAB/回顶按钮分别设唯一 heroTag
     ('clipSelectionFab' / 'clipBackToTopFab')，隐蔽按钮用 IgnorePointer +
     AnimatedOpacity，既保留淡入淡出动画又不吞手势。
涉及文件：lib/app/widgets/clip/clip_multi_selection_fab.dart、
          lib/app/widgets/clip_list_view/clip_list_fab.dart、
          lib/app/modules/views/windows/history/history_window.dart

问题2：点开右下角多选菜单后，扇形会"打开又自动关闭"
根因（已核对 flutter_expandable_fab 2.5.2 源码，expandable_fab.dart 第 231-240 行）：
      ExpandableFabState.didUpdateWidget 里无条件执行 close()：
        // Always reset FAB to closed state when widget is rebuilt
        close();
      而列表在滚动、刷新、选中项变化时都会 setState 重建整棵 FAB 子树，
      每次都生成一个新的 ExpandableFab 实例 → 触发 didUpdateWidget → 强制收起，
      表现为"刚点开就被关闭"。
修复：新增 _MultiSelectionFabHost（StatefulWidget）把 ExpandableFab 缓存在 State
      字段 _fab 中，并在 build 里复用同一个实例。Flutter 的 updateChild 遇到
      newWidget == oldWidget（同一实例引用）会走快路径直接跳过更新，
      didUpdateWidget 不再被触发，展开状态得以保持。
      仅在真正影响几何/内容/可用性的输入变化时才重建缓存：
      distance 变化，或动作列表的"数量 / 禁用态 / 图标"变化（_sameActions 判断）。
涉及文件：lib/app/widgets/clip/clip_multi_selection_fab.dart

问题3：文件传输长按删除弹窗底部按钮文字溢出边界
根因：Global.showTipsDialog（lib/app/utils/global.dart）把"中性/取消/确定"三个
      按钮全部塞进**单独一个 Row** 再作为 AlertDialog.actions 的唯一子项。
      而 AlertDialog 本会把 actions 交给 OverflowBar 布局，宽度不足时自动转为
      纵向排列；塞进一个 Row 后 OverflowBar 只看到"一个超宽子项"，
      永远不会溢出到纵向，导致窄屏上"连带文件删除 / 取消 / 仅删除记录"
      （合计 13 个汉字 + 三个按钮的内边距）超出弹窗边界、文字被裁切/溢出。
      该弹窗正是文件传输页同步文件长按删除时调用的
      （sync_file_page.dart → neutralText: 连带文件删除、okText: 仅删除记录）。
修复：把三个按钮改为 actions 的**独立子项**交给 OverflowBar 处理，
      并显式声明动作区布局：
        actionsAlignment: MainAxisAlignment.spaceBetween       // 宽屏：两端对齐
        actionsOverflowAlignment: OverflowBarAlignment.end    // 换行后右对齐
        actionsOverflowDirection: VerticalDirection.down      // 纵向堆叠
        actionsOverflowButtonSpacing: 4
      Visibility 补全 maintainSize/maintainAnimation/maintainState（均为 false，
      满足 Visibility 的构造断言，隐藏时不占位）；
      按钮文字加 textAlign: TextAlign.center。窄屏自动换行，文字不再越界，
      宽屏仍保持"左侧中性按钮、右侧确认按钮"的原排布意图。
涉及文件：lib/app/utils/global.dart

【验证说明（如实）】
- 已通读 Flutter 3.35.1 官方源码确认：AlertDialog 用 OverflowBar 包裹 actions，
  OverflowBar 在子项总宽超过可用宽度时会切换为纵向布局（overflow_bar.dart
  RenderOverflowBar.performLayout 第 522-541 行），本修复方向与框架行为一致。
- 已核实 flutter_expandable_fab 2.5.2 全部用到的构造参数存在且类型正确
  （distance / type / fanAngle / overlayStyle / openButtonBuilder /
  closeButtonBuilder / children），并确认 didUpdateWidget -> close() 的源码行为。
- 已逐项核对改动引用的符号均真实存在：TranslationKey 的
  deselect/cancelSelectAll/selectAll/copyMergedContent/copySuccess/output/delete/
  backToTop/deleteWithFiles/onlyDeleteRecordsText 等、NumberExt 的 .ms 扩展、
  ClipListView.list 为 List<ClipData>、CompactClipData.data 为 ClipData、
  ClipMultiSelectionController 的 allSelected/selectAll/toggleSelectAll 签名。
- 修正的连带问题：历史弹窗中原本 build() 内的局部变量 canMergeCopy 在新抽取的
  _buildSelectionFab() 方法中会失作用域，已改为在方法内自行计算，
  并移除 build() 中因此产生的未使用局部变量。
- 括号/花括号结构已用脚本逐字符校验平衡通过。
- 说明：本沙箱内的 QEMU 用户态模拟无法运行 Dart VM（JIT/AOT 快照执行均触发
  QEMU internal SIGSEGV），因此本轮**未能**在本机实际执行 flutter analyze /
  kernel_snapshot 编译；改动为源码级核对，仍建议在正常 PC 上跑一次
  flutter analyze 与构建确认。

【2026-09-17 补充】取消选中最后一项时自动退出多选模式
--------------------------------------------------------
问题：多选模式下逐条取消选中，当取消到最后一项（选中数归零）时，界面仍停留在
      多选状态——底部多选工具栏 / 计数为 0 的多选界面不会消失，需要额外点一次
      关闭或返回键才能回到原始列表状态。

根因：toggleItem / selectRange / removeMissingItems 只增删选中集合，从不检查
      "集合是否已空"。选中数归零后 enabled 仍为 true，UI 继续按多选渲染。

改动文件（2 个）：
1) lib/app/widgets/clip/clip_multi_selection_controller.dart
   - toggleItem：取消选中后调用新增的私有方法 _exitIfNothingSelected()，
     集合为空时将 _enabled 置 false 自动退出多选。
   - selectRange：末尾补充 _exitIfNothingSelected() 兜底（早期分支走 toggleItem）。
   - removeMissingItems：清理失效项后同样调用 _exitIfNothingSelected()，
     解决"删掉全部已选项后残留计数为 0 的多选界面"。
   - 新增私有方法 _exitIfNothingSelected()，统一"无选中即退出"的判定，
     保证各入口语义一致。
   - 控制器内不触发重建，由调用方统一 setState（与既有约定一致）。

2) lib/app/widgets/clip_list_view.dart
   - _refreshState()：新增同步逻辑——若控制器已退出多选而全局开关
     appConfig.isEnableMultiSelectionMode 仍为 true，则调用
     disableMultiSelectionMode(true) 关闭，避免 PopScope / 返回键拦截、
     底栏多选 UI 与选中数为 0 的状态不一致。
   - didUpdateWidget()：列表尾部数据变化触发 removeMissingItems 后，
     同样同步关闭全局多选开关（该路径不经 _refreshState）。

作用范围：主列表与历史弹窗两个界面共用同一控制器，改动在控制器层统一生效，
          无需修改任何调用点。取消最后一项选中（点击卡片或点击勾选框）均自动
          回到原始状态；已取消选中项的界面不再出现"空多选"悬挂状态。

影响面：仅新增 2 个判断分支 + 1 个私有方法，不改变选中/全选/区间选择/删除等
        既有逻辑，无接口签名变更。

【2026-09-17 补充2】多选计数徽标字号字重回调
--------------------------------------------------------
调整 lib/app/widgets/clip/clip_multi_selection_fab.dart 计数徽标（'已选 / 总数'）：
- 字号 18 -> 20（恢复与原始版本一致的字号）
- 移除 fontWeight: FontWeight.w600（此前新增的加粗），恢复默认常规字重，不再加粗
- 容器高度 44 -> 48、圆角 22 -> 24（胶囊形）、水平内边距 14 -> 16，
  按 20 号字重新配平，保证数字上下左右留白均匀、与主 FAB（56）比例协调

【2026-09-17 补充3】计数徽标全部恢复原始样式
--------------------------------------------------------
按用户要求，lib/app/widgets/clip/clip_multi_selection_fab.dart 计数徽标
（"已选 / 总数"）的全部外观参数恢复为与原始版本完全一致：
- fontSize:          20（保持）
- fontWeight:        不设置（默认常规，不加粗）
- Container height:  48
- BorderRadius:      circular(8) 圆角矩形
- 水平内边距:        10
说明：此前"补充2"中调整为 48/24/16 的胶囊风格已全部回退，徽标外观与
      原始版本逐项一致。徽标外部新增的淡入淡出（AnimatedOpacity）与
      定位计算不属于外观样式，保持不变。

【2026-09-17 补充4】文件传输删除弹窗精简
--------------------------------------------------------
需求：文件传输长按/多选删除的弹窗原有三个按钮（连带文件删除 / 取消 / 仅删除记录），
      按钮偏多且"删除原文件"这个次要选项占据了一个主按钮位；改为把该选项做成
      内容区的勾选小框，按钮精简为两个。

改动文件（3 个）：
1) lib/app/modules/sync_file_module/sync_file_page.dart
   - 删除弹窗由三按钮改为两按钮：okText 改为"删除"，取消 showNeutral，
     移除 neutralText / onNeutral。
   - 内容区通过 customWidget 增加 CheckboxListTile「同时删除已发送的原文件」，
     状态用 RxBool deleteFiles 管理，默认不勾选。
   - onOk：controller.deleteRecord(deleteFiles.value) —— 勾选则连带删除原文件，
     不勾选则仅删除记录。
   - 弹窗正文由 syncingFilePageDeleteSelectedDialogContent 改为 multiDeleteAsk
     （"是否删除选中的 @length 项？"）。原副标题"发送记录的文件不会被删除"
     与新增的勾选框语义冲突，已移除；同时与主列表删除弹窗保持文案一致。
   - 实现方式与主列表删除弹窗（clip_list_view.dart 的 CheckboxListTile +
     customWidget 模式）保持统一。

2) lib/app/data/enums/translation_key.dart
   - 新增枚举 deleteWithSourceFiles。

3) lib/app/translations/zh_cn_translations.dart / en_us_translations.dart
   - 新增文案：
     中文 "同时删除已发送的原文件" / 英文 "Also delete the sent source files"。

说明：syncingFilePageDeleteSelectedDialogContent 与 onlyDeleteRecordsText 两个
      翻译 key 失去引用（保留 enum 与翻译条目，未删除，不影响编译与其他调用）。

【2026-09-17 补充5】多选撤回后偶发退出应用 / 长按震动两下 修复
--------------------------------------------------------
问题现象：
  a) 多选状态下选中若干项后再撤回选中，有时会直接退出应用；
  b) 异常时点按/长按会连续震动两下（正常应为一下）。

根因分析：
  1) 震动两下：ClipDataCardCompact 的 Slidable 侧滑补选入口参数过于激进——
     extentRatio: 0.01、dismissThreshold: 0.1，阈值近似为零。
     长按进入多选时手指必然存在微小位移，会被 DismissiblePane 误判为"侧滑补选"，
     于是同一次手势内 onLongPress 与 onToggleSelected 先后触发，各自调用
     HapticFeedback.mediumImpact()，表现为震动两下；同时 onToggleSelected
     会重算选中集合（selectRange），可能把刚建立的多选态意外清空。
  2) 偶发退出应用：多选态下按边缘返回时，只有外层 HomePage 的 PopScope
     在做拦截，而它依赖 appConfig 全局多选开关。该开关可能因列表刷新、
     控制器自动退出等时序被提前置为 false，一旦判断失败，返回手势会穿透到
     外层"二次返回退出应用"逻辑，累计计时后用户再返回一次即真的退出。

改动文件（4 个）：

1) lib/app/widgets/clip/clip_data_card_compact.dart
   - 新增 _lastLongPressAt 时间戳与 _longPressGuardWindow（600ms）时间窗，
     以及 _isInLongPressGuard 判定。
   - onLongPress 包装：先记录时间戳再调用原回调；新增 onTapDown 清空时间戳，
     保证每个新手势正常重置，不影响正常的侧滑补选。
   - Slidable 参数放宽：extentRatio 0.01 → 0.15，dismissThreshold 0.1 → 0.15，
     只有明确的横向滑动才会触发补选入口。
   - confirmDismiss 内新增守卫：处于长按时间窗内则直接返回 false，不重复补选。
     用时间窗而非布尔标记，避免标记未复位导致侧滑补选永久失效。

2) lib/app/widgets/clip_list_view.dart
   - build 外层由原本直接返回 Scaffold，改为用 PopScope 包裹。
   - canPop: !_selectionController.enabled —— 只要本地多选态仍打开，就吃掉这次
     返回并调用 _exitSelectionMode() 清空多选，不再依赖 appConfig 全局开关，
     从根本上杜绝返回手势穿透导致的"退出应用"。
   - 清空多选后调用 Get.find<HomeController>().markBackGestureConsumed()，
     通知外层本轮返回已被内层消费。

3) lib/app/modules/home_module/home_controller.dart
   - 新增 _lastConsumedBackAt 时间戳与 markBackGestureConsumed() /
     wasBackGestureRecentlyConsumed() 两个方法。
   - 采用时间戳（500ms 窗口）而非布尔标记：嵌套 PopScope 的回调都注册在同一个
     Set 中，遍历顺序不保证，布尔标记在"外层先执行"时会读到旧值而失效。
   - 该判定为只读不改写，以适配两种执行顺序。

4) lib/app/modules/home_module/home_page.dart
   - onPopInvoked 开头新增判定：若内层刚消费过本轮返回，则直接 return，
     避免把"清空多选"误计入"再按一次退出应用"的计时。

收敛性说明（两种回调顺序均已推演）：
  - 内层先执行：内层清空多选并打时间戳 → 外层读到时间戳直接 return。
  - 外层先执行：全局开关此时仍为 true → 外层正常 disableMultiSelectionMode
    并通知内层清空 → 内层再收到时 enabled 已为 false，不重复处理。
  两种顺序都收敛到"清空多选且不累加退出计时"。
  非多选态下内层 canPop=true、不拦截，外层逻辑与改动前完全一致。

⚠ 待真机验证：
  Flutter 在 popDisposition 为 doNotPop 时，会禁用 CupertinoPageRoute 的边缘
  滑动手势（routes.dart:1900）。Android 使用 MaterialPageRoute，其返回手势
  依赖系统预测性返回，行为不同，本沙箱无法运行 Dart VM，未能实测。
  请在真机上确认：多选态下边缘返回是否清空多选且不退出应用。


================================================================================
【2026-09-19】修复「长按震动两下」与「点一下选中多项、计数不涨」（复现问题）
================================================================================

■ 问题现象（用户复现）
  1. 历史记录列表中，长按偶尔会出现两次震动，手感"稀碎"。
  2. 偶尔点一下会选中多项（列表明明只点了几下，底部计数却显示 11 / 122），
     且蓝色选中框与计数对不上。

■ 上一版为何没修好
  上一版（v4）修改的是 ClipDataCard（主列表用），但历史记录窗口实际使用的是
  ClipDataCardCompact，且在 history_window.dart 的 itemBuilder 中**内联**了
  onTap / onLongPress / onToggleSelected 三个回调。
  也就是说：上一版改的文件，历史窗口根本没走到，属于"改错了地方"。
  本轮直接定位到真实生效的代码路径。

■ 根因（共 3 层，本次全部修复）

  根因 A：double 震动 —— onToggleSelected 内也调用了 HapticFeedback
    history_window.dart 的 onToggleSelected 中原本有：
        HapticFeedback.mediumImpact();
    当长按抬手时的微小位移被 Slidable 判定为侧滑时，onLongPress（震 1 次）
    与 onToggleSelected（震 2 次）在同一次手势内**先后都触发**，于是震动两下。
    防线的 _isInLongPressGuard（600ms）窗口偏紧，长按后保持按住稍久再抬手
    就会越过窗口，守卫失效。

  根因 B：selectRange 的"区间填充"语义 —— 点一下选中一大片
    onToggleSelected 调用的是 ClipMultiSelectionController.selectRange，
    而该方法在"已有选中项 + 新项不在选中集"时会**把两点之间的整段全部选中**。
    任何一次误触发都会一次性选中十几项，这正是计数 11 / 122 的来源，
    也是"计数与蓝框对不上"的直接原因。

  根因 C：卡片维护了第二份选中状态（ClipDataCard）
    build() 中 `_selected = widget.selected;` 会把内部值强制重置为外部值，
    同时 onTap 里 `setState(() => _selected = !_selected)` 又自行翻转一次，
    形成"内部 UI 状态"与"外层真实选中集合"两套状态互相覆盖，蓝框与计数分叉。
    另外 DoubleTapWrapper 在多选态下仍走 200ms 双击延迟，点击后需等待才响应，
    表现为"点了没反应、计数不更新"。

■ 修改清单（5 个文件）

  1) lib/app/modules/views/windows/history/history_window.dart（历史窗口真正生效处）
     - onTap：多选态下只做单项 toggleItem，不再调用震动（避免与长按叠加）。
     - onLongPress：保留为进入多选 / 选中当前项的**唯一入口**，也是**唯一震动源**。
     - onToggleSelected：删除 HapticFeedback 调用；并将 selectRange 改为
       toggleItem —— 严格单项切换，选中结果与计数、蓝框始终一一对应。
       彻底消除"点一下选中一大堆"。

  2) lib/app/widgets/clip_list_view/clip_list_item_renderer.dart（主列表渲染器）
     - onToggleSelected：同样删除 HapticFeedback 与 selectRange，改为单项 toggleItem。
     - 现在整个剪贴板多选链路中 selectRange 已无调用方（仅剩方法定义）。

  3) lib/app/widgets/clip/clip_data_card.dart
     - 删除内部 _selected 字段，改为完全受控组件（统一读取 widget.selected）。
     - build() 移除 `_selected = widget.selected;` 的强制重置。
     - leftTapWrapper.onTap 不再自行 setState 翻转，只透传 widget.onTap。
     - onDoubleTap 改为运行时判断 widget.selectMode，多选态下不响应双击复制，
       也不再受 DoubleTapWrapper 的 200ms 延迟影响。
     - _longPressGuardWindow 600ms → 1000ms（覆盖"长按判定 → 抬手"整段）。

  4) lib/app/widgets/clip/clip_data_card_compact.dart（历史窗口实际使用的卡片）
     - _longPressGuardWindow 600ms → 1000ms，并补充说明取值依据。
     - confirmDismiss 守卫保留，配合窗口放宽后可靠屏蔽长按抬手误触发。

  5) lib/app/widgets/clip/clip_multi_selection_controller.dart
     - 未改动。selectRange 方法保留（供后续可能的"长按拖动区间选择"复用），
       但当前 UI 已不再调用它。

■ 修复效果
  - 震动：多选链路的 HapticFeedback 已收敛为每处仅 onLongPress 内 1 次，
    长按不再出现两下震动（已用 grep 全项目校验，仅 2 处、各 1 次）。
  - 选中：任何点击 / 补选动作都只影响当前一项，计数与实际选中项严格一致，
    不会再出现"点一下选中 11 项、计数对不上"的情况。

■ 待真机验证
  本沙箱无法运行 Dart VM（JIT/AOT 会触发 QEMU SIGSEGV），无法执行
  flutter analyze 与构建。请在有 Flutter 环境的 PC 上执行：
      flutter analyze
      flutter build apk --release
  并在真机上确认：
    1) 历史记录长按 → 仅震动一次。
    2) 多选态下点击某项 → 只有该项被选中，底部计数 +1。
    3) 长按选中后继续点其他项 → 计数逐个递增，与蓝框一致。

════════════════════════════════════════════════════════════════════
【2026-09-19 补充·根因修正】长按"快速震动两下"的真正原因
════════════════════════════════════════════════════════════════════

■ 用户反馈的关键信息
  "手指没有位移，就是长按之后偶尔会快速震动两下，是紧挨着的快速两下，
   不是隔很久的两下。"

■ 上一版判断错误（说明）
  上一版认为第二下震动来自 Slidable 把长按抬手误判为侧滑，
  于是去调 onToggleSelected / selectRange、并加长按守卫窗口。
  ——这个假设与"无位移、快速两下"的现象不符，属于误判。

■ 真实根因（已在 Flutter 源码中确认）
  ClipDataCard / ClipDataCardCompact 内部的 InkWell 没有显式设置
  enableFeedback，其默认值为 true。查阅 Flutter
  packages/flutter/lib/src/material/ink_well.dart 第 1225-1229 行：

      if (widget.onLongPress != null) {
        if (widget.enableFeedback) {
          Feedback.forLongPress(context);   // 第 1 次震动
        }
        widget.onLongPress!();              // 回调内又震一次 → 第 2 次
      }

  而 Feedback.forLongPress 在 Android 上执行的是
  HapticFeedback.vibrate()（见 flutter/lib/src/widgets/feedback.dart），
  我方回调里又调用了 HapticFeedback.mediumImpact()。

  两者仅相隔几毫秒 → 表现为"长按快速震动两下"，
  且与手指是否位移完全无关，正好吻合用户描述。

  之所以是"偶尔"：当长按手势被其他识别器竞争掉时，
  InkWell.onLongPress 不触发，就不会叠加成两下。

■ 本次修复
  1) lib/app/widgets/clip/clip_data_card_compact.dart
     - InkWell 显式设置 enableFeedback: false
       → 关闭系统自带的 Feedback.forLongPress（Android = vibrate()）。
     - onLongPress 回调内显式调用一次 HapticFeedback.mediumImpact()，
       作为多选的统一震动反馈（保留 mediumImpact 的明显手感）。
     - 新增 _lastHapticAt 字段 + 300ms 时间窗去重：
       个别 ROM 可能重复派发长按回调，此处兜底，保证"一次长按只震一次"。
     - 新增 import 'package:flutter/services.dart'。

  2) lib/app/widgets/clip/clip_data_card.dart（主列表卡片）
     - 同样设置 enableFeedback: false；
     - onLongPress 内显式单次 mediumImpact + _lastHapticAt 去重；
     - 新增 import 'package:flutter/services.dart'。

  3) lib/app/modules/views/windows/history/history_window.dart
     - 移除 onLongPress 内原先手动添加的 HapticFeedback（会与组件内叠加）。
     - 震动改为统一由 ClipDataCardCompact 负责，此处只做状态变更。

  4) lib/app/widgets/clip_list_view/clip_list_item_renderer.dart
     - 移除 onLongPress 手动震动，理由同上；
     - 更新注释，说明震动归属。

■ 最终震动归属（全项目校验）
  - 多选 / 长按链路：恰好 2 处 HapticFeedback.mediumImpact()，
    分别位于两个卡片组件的 onLongPress 内，各自带时间窗去重。
  - 两处 InkWell 均为 enableFeedback: false，系统不再额外震动。
  - 因此单次长按 → 单次震动。

■ 顺带保留
  - 多选态点击只切换单项（toggleItem），不再触发 selectRange，
    计数与蓝框严格一一对应 → 修复"点一下选中多项、计数不增加"。

■ 侧滑（补选）功能：保持原样，未做任何改动
  排查过程中曾一度怀疑"长按抬手时的微小位移被 Slidable 误判为侧滑"，
  并据此改过 extentRatio / dismissThreshold，还加过 _longPressGuardWindow
  时间窗来屏蔽。但用户反馈明确是"无位移、紧挨着快速两下"，
  该假设与现象不符，已在最终版本中【全部回滚】：

    - extentRatio:    0.15  → 恢复 0.01（与原版一致）
    - dismissThreshold: 0.15 → 恢复 0.1 （与原版一致）
    - confirmDismiss: 移除 _isInLongPressGuard 守卫，恢复直接调用 onToggleSelected
    - 移除 _lastLongPressAt / _longPressGuardWindow / onTapDown 清空逻辑

  即：侧滑补选的灵敏度与触发时机与原始版本完全相同，
  本次修复只针对震动来源（InkWell.enableFeedback）与选中语义（toggleItem）。

■ 待真机验证（同前）
  本沙箱无法运行 Dart VM，请在有 Flutter 环境的 PC 上执行
  flutter analyze 与构建后，于真机确认：
    1) 长按历史记录 → 只震动一下（快速两下的问题应消失）。
    2) 多选态点击某项 → 只选中该项，底部计数 +1。
    3) 侧滑补选 → 行为与原始版本一致（灵敏度未变）。

════════════════════════════════════════════════════════════════════
【2026-09-19 补充·UI 统一】历史记录计数 与 文件传输计数 样式对齐
════════════════════════════════════════════════════════════════════

■ 问题
  用户反馈：历史记录窗口的"1 / 8"计数，与文件传输页的计数看起来
  "有点区别"，怀疑其中一个被加黑过。

■ 排查结果
  两处计数分别位于：

  (A) 历史记录窗口
      lib/app/widgets/clip/clip_multi_selection_fab.dart 第 107 行附近
      Container(height: 48, borderRadius: 8, padding horizontal: 10)
        color:    colorScheme.primaryContainer      ← 跟随主题
        TextStyle(fontSize: 20, color: colorScheme.onPrimaryContainer)

  (B) 文件传输页
      lib/app/modules/sync_file_module/sync_file_page.dart 第 138 行附近
      Container(height: 48, borderRadius: 8, padding horizontal: 10)
        color:    const Color(0xffc3e8ff)           ← 硬编码浅蓝
        TextStyle(fontSize: 20,
                  color: 暗色?Colors.white:Colors.black87)

  → 字号(20)、高度(48)、圆角(8)、内边距(10) 四项完全一致，
    唯一差异是【配色】：
      - (A) 用主题色（本应用 seedColor = Colors.lightBlueAccent，
            生成的 primaryContainer 偏青的浅蓝，文字为 onPrimaryContainer）
      - (B) 用硬编码 #c3e8ff（纯浅蓝），文字为 black87 / white
    两者色相与明度略有差别，视觉上就会觉得"其中一个更重"。
    并非字重(fontWeight)差异 —— 两处均未设置 fontWeight，均为默认 w400。

■ 修复
  统一改为使用主题色，具体改动 lib/app/modules/sync_file_module/sync_file_page.dart：
    - 背景：const Color(0xffc3e8ff)  →  Theme.of(context).colorScheme.primaryContainer
    - 文字：暗色白/亮色 black87      →  Theme.of(context).colorScheme.onPrimaryContainer
    - 其余属性（height 48 / radius 8 / padding 10 / fontSize 20）保持不变。

  这样两处计数徽标的背景色、文字色、字号、字重、圆角、内边距全部一致，
  并且在亮色 / 暗色主题下都会自动跟随主题配色。

■ 说明
  两处的数值来源仍是各自独立的：
    历史记录窗口 → ClipMultiSelectionController.selectedCount / 列表长度
    文件传输页   → controller.selected.length / controller.recHistories.length
  本次只统一"外观样式"，不改变任何计数逻辑。

════════════════════════════════════════════════════════════════════
【2026-09-19 补充·深色模式】文件传输进度条文字颜色未跟随主题
════════════════════════════════════════════════════════════════════

■ 问题
  用户反馈：文件传输页在深色模式下，"这个色没有变"。

■ 排查结果
  定位到 lib/app/widgets/sync_file_status.dart 的进度条双色文字：

      SegmentedTextColorContainer(
        segmentedColor: Colors.white,                            ← 已填充部分
        widthFactor: value,
        defaultTextStyle: const TextStyle(color: Colors.black),  ← 未填充部分
        ...
      )

  该组件（lib/app/widgets/segmented_color.dart）通过按进度比例裁剪两层文字，
  实现"进度条左半白字、右半黑字"的效果：
    - 底层 使用 defaultTextStyle（原为硬编码 Colors.black）
    - 上层 按 widthFactor 裁剪，使用 segmentedColor（Colors.white）

  问题在于 defaultTextStyle 被【硬编码为黑色】，完全不跟随主题。
  LinearProgressIndicator 未指定 backgroundColor，采用的是 Material 3 默认值：
    - 亮色模式：底轨浅灰 → 黑字可见 ✅
    - 深色模式：底轨深灰 → 黑字几乎不可见 ❌
  这正是"深色模式下这个色没有变"的原因。

■ 修复
  lib/app/widgets/sync_file_status.dart
    defaultTextStyle:
        const TextStyle(color: Colors.black)
      ↓ 改为
        TextStyle(color: Theme.of(context).colorScheme.onSurface)

  说明：
    - onSurface 在亮色模式为深色文字、暗色模式为浅色文字，
      与各自的底轨颜色都有足够对比度。
    - segmentedColor 仍保留 Colors.white：进度条填充色为 colorScheme.primary
      （亮色为蓝、暗色为浅蓝），白字在其上对比度在两种模式下均可接受。
    - 未改动 LinearProgressIndicator 的默认配色，保持与全局主题一致。

■ 关联改动（本次一并交付）
  同文件内的其它硬编码颜色（复选框 #33A0E3、上传/下载图标蓝绿、
  停止红、重发橙、文件夹灰）属于语义色，深浅模式下均可辨识，
  本次未做改动，避免过度调整。

■ 仍在文件传输页的其它硬编码（已确认不影响可读性，暂留）
  - sync_file_page.dart:89  Border.all(color: selected ? Colors.blueGrey : Colors.white, width: 0)
    → 宽度为 0，边框实际不渲染，无视觉影响。
  - pending_file_list.dart 中的 Colors.blueGrey（次要文字/图标色），
    在深色背景下对比度尚可。

════════════════════════════════════════════════════════════════════
【2026-09-19 补充·回滚】侧滑参数 与 回到顶部按钮尺寸
════════════════════════════════════════════════════════════════════

■ 一、侧滑（区间补选）参数：全部回滚为原版

  回滚原因：
    排查"长按震动两下"时，曾错误假设"长按抬手时的微小位移被 Slidable
    误判为侧滑补选"，据此改动了侧滑参数并加了长按守卫时间窗。
    但用户反馈明确是"无位移、紧挨着快速两下"，该假设与现象不符。
    后来在 Flutter 源码中确认真实根因是 InkWell.enableFeedback
    （系统反馈与手动震动叠加），与侧滑完全无关。

  回滚内容（clip_data_card.dart / clip_data_card_compact.dart）：
    - extentRatio:       0.15  → 0.01（原版）
    - dismissThreshold:  0.15  → 0.1 （原版）
    - confirmDismiss:     移除 _isInLongPressGuard 守卫，恢复直接调用 onToggleSelected
    - 删除字段与逻辑：_lastLongPressAt / _longPressGuardWindow / _isInLongPressGuard
    - 删除 onTapDown 中清空时间窗的代码

  结果：侧滑补选的灵敏度与触发时机与原始版本完全一致。

■ 二、回到顶部按钮尺寸：恢复为原版

  问题：按钮被改小了（用户反馈"回到置顶那个图标也变小了"）。

  原因：做"折叠态布局统一"时，为与主 FAB 对齐，误将标准尺寸改成了小尺寸：
      FloatingActionButton.small(...)   // 直径 40（错误）
      FloatingActionButton(...)         // 直径 56（原版）

  恢复内容（clip_multi_selection_fab.dart）：
    - FloatingActionButton.small → FloatingActionButton（标准尺寸，直径 56）
    - 位置恢复原版数值：right: 15，bottom: selectMode ? 85 : 15
    - 计数徽标位置恢复原版数值：right: 85，bottom: 15
    - 清理不再使用的 _badgeGap 常量

■ 三、保留项（明确说明）
  clip_multi_selection_fab.dart 中保留以下改动：
    - _MultiSelectionFabHost：修复"扇形展开后自动关闭"（复用同一 widget 实例，
      规避 flutter_expandable_fab 的 didUpdateWidget 强制 close）。
    - IgnorePointer + AnimatedOpacity：显隐改为淡入淡出（用户已确认保留）。
    - heroTag: 'clipSelectionFab'：避免多窗口 hero 标签冲突。
    - fanAngle: 90：扇形角度显式固定。
  上述改动均不改变按钮尺寸与位置。

■ 最终震动链路（全项目）
  - 多选/长按链路中 HapticFeedback 仅 2 处，位于两个卡片组件的 onLongPress 内，
    各带 300ms 时间窗去重。
  - 两处 InkWell 均 enableFeedback: false，系统不再额外震动。
  - 单次长按 → 单次震动。

════════════════════════════════════════════════════════════════════
【2026-09-19 修正·重要】侧滑补选功能被误删 —— 已恢复
════════════════════════════════════════════════════════════════════

■ 问题
  用户反馈"侧滑补选根本没修复，现在都没用了"。

■ 根因（两处，均为本人误改）
  之前把 onToggleSelected 的回调语义从 selectRange（区间补选）
  改成了 toggleItem（单项切换），导致侧滑只能切换单项，
  区间补选功能实际被移除。

  误改原因：排查"点一下偶尔选中一大堆"时，误以为根因是 selectRange
  的区间填充逻辑；实际根因是长按双震动（InkWell.enableFeedback 叠加），
  与 selectRange 无关。属于基于错误假设改动功能，已深刻检讨。

■ 恢复内容

  1) lib/app/modules/views/windows/history/history_window.dart（约 407 行）
     onToggleSelected：
       - 移除：toggleItem(item.data) + "已启用才执行"的短路 return
       - 恢复：
           if (!_selectionController.enabled) { _enableSelectMode(); }
           _selectionController.selectRange(
             _list.map((e) => e.data).toList(growable: false), item.data);
           _refreshState();

  2) lib/app/widgets/clip_list_view/clip_list_item_renderer.dart（约 73 行）
     onToggleSelected：
       - 移除：toggleItem(item)
       - 恢复：
           if (!_selectionController.enabled) { _enableSelectMode(); }
           _selectionController.selectRange(
             List<ClipData>.from(widget.list), item);
           _refreshState();

  说明：selectRange 本身自带保护 —— 未进入多选态时只执行 _enableSelectMode()，
  不进行区间填充；因此侧滑的手感与原始版本一致。

■ 同时确认（未改动，与原版一致）
  - clip_data_card_compact.dart 侧滑参数：extentRatio 0.01、dismissThreshold 0.1、
    confirmDismiss 直调 onToggleSelected，均与原版一致。
  - clip_data_card.dart 侧滑逻辑与原版一致。
  - 两个卡片组件仅有两处改动：InkWell enableFeedback: false、震动 300ms 时间窗去重，
    均不影响侧滑功能。

■ 两条侧滑路径
  - 历史窗口：history_window.dart → ClipDataCardCompact（compact 卡片）
  - 列表视图：clip_list_item_renderer.dart → ClipDataCard（标准卡片）
  两条路径的 onToggleSelected 均已恢复 selectRange。
